/** @type {import("prettier").Config} */
const config = {
  trailingComma: 'es5',
  tabWidth: 2,
  semi: true,
  singleQuote: true,
  arrowParens: 'avoid',
  printWidth: 80,
  quoteProps: 'as-needed',
  bracketSameLine: true,
};

export default config;
